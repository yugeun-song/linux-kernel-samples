# ---- Kernel module build artifacts (kbuild) ----
*.o
*.o.*
*.ko
*.ko.unsigned
*.mod
*.mod.c
*.mod.o
*.symvers
*.order
Module.symvers
modules.order
.tmp_versions/
.*.cmd
*.dwo
*.gcno

# ---- Generated kbuild stub + isolated per-sample build dirs (build time, never committed) ----
Kbuild
.build-*/

# ---- PC-local build target config (not part of the repo) ----
config.mk

# ---- Module-signing key material (never commit) ----
MOK.*
signing_key.*
*.priv
*.key
*.pem
*.der
*.cer
*.crt
*.x509
*.p12
*.pfx

# ---- Other private keys / secrets (never commit to a public repo) ----
id_rsa
id_dsa
id_ecdsa
id_ed25519
*.gpg
*.keystore
*.jks
.env

# ---- clangd / LSP ----
compile_commands.json
.cache/
.clangd/

# ---- Editor / code indexing ----
*.swp
*.swo
*~
tags
.tags*
cscope.files
cscope.out
cscope.in.out
cscope.po.out

# ---- Core dumps (userspace test harnesses, if any) ----
core
core.*

# ---- OS ----
.DS_Store
Thumbs.db

# ---- AI tool artifacts ----
.claude/
CLAUDE.md
